{
  "name": "typescript-type-completion-sorting-plugin",
  "version": "1.0.0",
  "description": "Push inherited properties from base types to bottom for TS completion",
  "main": "./index",
  "scripts": {
    "clean": "del-cli '*.js' '*.map' '*.d.ts'",
    "build": "tsc",
    "prepublish": "npm run clean && npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "del-cli": "^1.1.0",
    "typescript": "^2.5.3"
  },
  "peerDependencies": {
    "typescript": ">= 2.3.1 < 3"
  }
}
